[project]
name = "driftguard"
version = "0.1.0"
description = "Agent Drift evaluation guardrail MVP"
requires-python = ">=3.9"

[project.scripts]
driftguard = "driftguard.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
